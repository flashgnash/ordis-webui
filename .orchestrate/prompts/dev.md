Implement {{ticket.id}} in the provisioned workspace from {{workspace.inputCommit}} on {{workspace.ticketBranch}}.

Requirements:
{{ticket.description}}

Inspect the repository, implement the ticket, commit all completed work, leave the worktree clean, then run `ticketctl complete`.
