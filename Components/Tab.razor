@code {
    [CascadingParameter] TabList Parent { get; set; } = null!;

    [Parameter] public string Name { get; set; } = "";
    [Parameter] public RenderFragment? ChildContent { get; set; }

    protected override void OnInitialized()
        => Parent.Register(this);
}
